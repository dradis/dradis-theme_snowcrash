<div class="header">
  <div class="header-inner">
    <div class="options">
      <div class="dropdown">
        <%= link_to raw('<i class="icon-plus"></i>'), '#', :class => 'dropdown-toggle', :data => {:toggle => 'dropdown'}%>
        <ul class="dropdown-menu pull-right" role="menu">
          <li><%= link_to 'Empty (no template)', new_issue_path, class: 'view-toggle-off editor-toggle', tabindex: '-1' %></li>
          <!-- <li class="divider"></li> -->
          <%# if NoteTemplate.all.any? %>
          <%# NoteTemplate.all.each do |template| %>
          <%# end %>
          <%# else %>
          <!-- <li class="disabled"><a href="#">(No templates defined)</a></li> -->
          <!-- <li><a href="#"><i class="icon-plus"></i> add new template</a></li> -->
          <%# end -%>
        </ul>
      </div>
    </div>
    <h3>Project issues</h3>
  </div>
</div>

<div class="note-list">
  <div id="issues" class="note-list">
    <% if @issues.any? %>
      <%= render partial: 'dradis/frontend/issues/issue', collection: @issues %>
    <% end %>
    <div class="list-item">
      <div class="note placeholder <%= 'hide' if @issues.any? %>">(nothing yet)</div>
    </div>
  </div>
</div>